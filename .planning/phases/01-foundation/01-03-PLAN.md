---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [src/app/layout.tsx, src/app/page.tsx, src/components/.gitkeep, src/lib/.gitkeep, src/data/.gitkeep, src/types/index.ts]
autonomous: true
---

<objective>
Create base layout and establish folder structure conventions.

Purpose: Set up the app shell and organize the codebase for scalable development.
Output: Working layout with proper metadata, organized folder structure with type definitions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app layout with metadata and fonts</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <action>
Update `src/app/layout.tsx`:

1. Configure metadata for SEO:
```typescript
export const metadata: Metadata = {
  title: 'iMBA Money and Banking',
  description: 'Interactive study guide for the Money and Banking course - University of Illinois Urbana-Champaign',
  keywords: ['money', 'banking', 'finance', 'iMBA', 'UIUC', 'economics'],
};
```

2. Add Inter font from next/font/google (clean, modern, award-winning sites use it)

3. Create a minimal app shell structure:
- Main container with min-h-screen
- Semantic HTML structure (header placeholder, main, footer placeholder)
- Apply base background and text colors via Tailwind

4. Update `src/app/page.tsx` with a simple welcome message:
- "iMBA Money and Banking" as h1
- Brief description text
- "8 modules coming soon" indicator
- Use Motion's fadeIn animation on the heading (import from lib/motion.ts)
- Mark component as 'use client' for Motion

Keep it minimal - design system comes in Phase 2.
  </action>
  <verify>npm run dev shows styled page with animated heading, metadata appears in page source</verify>
  <done>Layout configured with metadata, fonts, semantic structure. Page shows animated welcome message.</done>
</task>

<task type="auto">
  <name>Task 2: Establish folder structure and type definitions</name>
  <files>src/components/.gitkeep, src/lib/.gitkeep, src/data/.gitkeep, src/types/index.ts</files>
  <action>
Create organized folder structure:

```
src/
├── app/              # Next.js App Router pages (already exists)
├── components/       # Reusable UI components
│   ├── ui/          # Base UI components (buttons, cards, etc.)
│   └── modules/     # Module-specific components
├── lib/             # Utilities and configurations (motion.ts already here)
├── data/            # Static data (module content, quiz questions)
├── types/           # TypeScript type definitions
└── hooks/           # Custom React hooks
```

Create directories with .gitkeep files to preserve structure in git.

Create `src/types/index.ts` with base type definitions for the app:
```typescript
// Module types
export interface Module {
  id: number;
  title: string;
  description: string;
  icon: string;
  slug: string;
}

// Quiz types
export interface QuizQuestion {
  id: string;
  question: string;
  options: string[];
  correctAnswer: number;
  explanation: string;
}

// Flashcard types
export interface Flashcard {
  id: string;
  front: string;
  back: string;
  moduleId: number;
}

// Progress tracking
export interface UserProgress {
  completedQuizzes: string[];
  flashcardProgress: Record<string, number>;
  lastVisited: string;
}
```

These types establish contracts for future phases.
  </action>
  <verify>TypeScript compiles without errors (`npx tsc --noEmit`), folder structure visible in src/</verify>
  <done>Folder structure created, base types defined, project organized for scalable development</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` runs without errors
- [ ] `npm run build` succeeds (static export works)
- [ ] `npx tsc --noEmit` passes (no TypeScript errors)
- [ ] Page shows animated heading with Motion
- [ ] Folder structure created (components/, data/, types/, hooks/)
- [ ] Type definitions compile correctly
</verification>

<success_criteria>
- All tasks completed
- Layout has proper metadata and semantic HTML
- Inter font configured and loading
- Motion animation working on page
- Folder structure established
- Base TypeScript types defined
- Project builds successfully as static site
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
