---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, next.config.ts, postcss.config.mjs, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, .gitignore]
autonomous: true
---

<objective>
Initialize Next.js 15 project with TypeScript and Tailwind CSS v4.

Purpose: Establish the core project foundation with modern static site generation capabilities.
Output: Working Next.js project with TypeScript and Tailwind CSS configured.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, .gitignore</files>
  <action>
Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --turbopack` to initialize in current directory.

After creation:
1. Update `next.config.ts` to add `output: 'export'` for static site generation
2. Remove the default Next.js boilerplate from page.tsx - replace with simple "iMBA Money and Banking" heading
3. Keep the default layout.tsx structure but update metadata title to "iMBA Money and Banking"

Note: Use `--turbopack` flag for faster development builds (Rust-based bundler).
  </action>
  <verify>npm run dev starts without errors, http://localhost:3000 shows the heading</verify>
  <done>Next.js project runs with TypeScript, shows custom heading, configured for static export</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 with CSS-first approach</name>
  <files>src/app/globals.css, postcss.config.mjs</files>
  <action>
Tailwind v4 uses CSS-first configuration. Update globals.css:

1. Replace content with clean Tailwind v4 setup:
```css
@import "tailwindcss";

/* Custom CSS variables for design tokens will be added in Phase 2 */
```

2. Verify postcss.config.mjs has `@tailwindcss/postcss` plugin (create-next-app should have set this up)

3. Test Tailwind is working by adding a Tailwind class to the page heading (e.g., `text-4xl font-bold`)

Note: Tailwind v4 auto-scans the project, no `tailwind.config.js` needed for basic setup.
  </action>
  <verify>Page heading displays with Tailwind styling (large, bold text)</verify>
  <done>Tailwind CSS v4 working with CSS-first configuration, styles apply correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` completes successfully (static export)
- [ ] TypeScript compilation passes (`npx tsc --noEmit`)
- [ ] Tailwind classes apply correctly
- [ ] `out/` directory created with static HTML after build
</verification>

<success_criteria>
- All tasks completed
- Next.js 15 project initialized with App Router
- TypeScript configured and working
- Tailwind CSS v4 configured with CSS-first approach
- Static export configured (`output: 'export'`)
- Development server runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
