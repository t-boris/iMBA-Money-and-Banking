---
phase: 03-module-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/data/modules.ts, src/components/modules/ModuleCard.tsx, src/components/modules/index.ts]
autonomous: true
---

<objective>
Create the module data and ModuleCard component with interactive preview animations.

Purpose: Build the core module card that serves as the visual gateway to each course topic. Cards should feel alive with interactive previews that hint at the module's content.
Output: Module data file with 8 modules, ModuleCard component with hover animations and preview capabilities.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-module-navigation/03-CONTEXT.md

@src/types/index.ts
@src/components/ui/Card.tsx
@src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create module data with 8 Money & Banking modules</name>
  <files>src/data/modules.ts</files>
  <action>
Create the module data file with all 8 Money and Banking course modules.

```typescript
import { Module } from '@/types';

export const modules: Module[] = [
  {
    id: 1,
    title: 'Money and the Financial System',
    description: 'Understanding money, its functions, and how the financial system facilitates economic activity.',
    icon: '💰',
    slug: 'money-financial-system',
  },
  {
    id: 2,
    title: 'Interest Rates and Returns',
    description: 'Present value, yield to maturity, and the relationship between interest rates and bond prices.',
    icon: '📈',
    slug: 'interest-rates-returns',
  },
  {
    id: 3,
    title: 'Risk and Term Structure',
    description: 'Understanding risk premiums, default risk, and the yield curve.',
    icon: '📊',
    slug: 'risk-term-structure',
  },
  {
    id: 4,
    title: 'Banking and Financial Intermediation',
    description: 'How banks operate, manage risk, and their role in the economy.',
    icon: '🏦',
    slug: 'banking-intermediation',
  },
  {
    id: 5,
    title: 'Central Banking and the Fed',
    description: 'The Federal Reserve system, monetary policy tools, and central bank operations.',
    icon: '🏛️',
    slug: 'central-banking-fed',
  },
  {
    id: 6,
    title: 'Money Supply Process',
    description: 'How money is created, the money multiplier, and factors affecting money supply.',
    icon: '💵',
    slug: 'money-supply-process',
  },
  {
    id: 7,
    title: 'Monetary Policy',
    description: 'Goals of monetary policy, transmission mechanisms, and policy implementation.',
    icon: '⚖️',
    slug: 'monetary-policy',
  },
  {
    id: 8,
    title: 'International Finance',
    description: 'Exchange rates, balance of payments, and international monetary systems.',
    icon: '🌍',
    slug: 'international-finance',
  },
];
```

Each module has:
- Unique id (1-8)
- Descriptive title matching course content
- Brief description for card preview
- Emoji icon for visual distinction
- URL-friendly slug for routing
  </action>
  <verify>File created, TypeScript compiles with `npx tsc --noEmit`</verify>
  <done>8 modules defined with titles, descriptions, icons, and slugs matching Money & Banking curriculum.</done>
</task>

<task type="auto">
  <name>Task 2: Create ModuleCard component with interactive preview</name>
  <files>src/components/modules/ModuleCard.tsx, src/components/modules/index.ts</files>
  <action>
Create ModuleCard component that extends the base Card with module-specific features:

```typescript
'use client';

import { motion } from '@/lib/motion';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui';
import { Module } from '@/types';
import Link from 'next/link';
import { cn } from '@/lib/utils';

interface ModuleCardProps {
  module: Module;
  index?: number;
}

export function ModuleCard({ module, index = 0 }: ModuleCardProps) {
  return (
    <Link href={`/modules/${module.slug}`} className="block">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{
          delay: index * 0.1,
          type: 'spring',
          stiffness: 300,
          damping: 20
        }}
      >
        <Card
          variant="glass"
          hover
          className="h-full group relative overflow-hidden"
        >
          {/* Animated background gradient on hover */}
          <div className={cn(
            'absolute inset-0 opacity-0 group-hover:opacity-100',
            'bg-gradient-to-br from-primary-500/10 to-transparent',
            'transition-opacity duration-300'
          )} />

          <CardHeader className="relative">
            {/* Icon with pulse animation on hover */}
            <motion.span
              className="text-4xl mb-3 block"
              whileHover={{ scale: 1.1 }}
              transition={{ type: 'spring', stiffness: 400, damping: 10 }}
            >
              {module.icon}
            </motion.span>

            {/* Module number badge */}
            <span className="text-xs font-medium text-text-muted uppercase tracking-wider mb-1 block">
              Module {module.id}
            </span>

            <CardTitle className="group-hover:text-primary-500 transition-colors">
              {module.title}
            </CardTitle>
            <CardDescription>{module.description}</CardDescription>
          </CardHeader>

          <CardContent className="relative">
            {/* Preview hint that appears on hover */}
            <motion.div
              className="flex items-center gap-2 text-primary-500 text-sm font-medium"
              initial={{ opacity: 0, x: -10 }}
              whileHover={{ opacity: 1, x: 0 }}
            >
              <span>Explore module</span>
              <motion.span
                animate={{ x: [0, 4, 0] }}
                transition={{ repeat: Infinity, duration: 1.5, ease: 'easeInOut' }}
              >
                →
              </motion.span>
            </motion.div>
          </CardContent>
        </Card>
      </motion.div>
    </Link>
  );
}
```

Create barrel export:

```typescript
// src/components/modules/index.ts
export { ModuleCard } from './ModuleCard';
```

Key features:
- Staggered animation on load (using index prop)
- Subtle gradient overlay on hover
- Icon pulse animation
- Module number badge for quick identification
- "Explore module" hint with animated arrow
- Glass morphism with the existing Card component
- Links to module page via slug
  </action>
  <verify>Component renders, hover animations work, TypeScript compiles</verify>
  <done>ModuleCard with interactive preview, hover effects, and staggered load animation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` starts without errors
- [ ] `npm run build` succeeds
- [ ] Module data exports 8 modules
- [ ] ModuleCard renders with glass effect
- [ ] Hover shows gradient overlay and icon animation
- [ ] Staggered load animation works with index prop
</verification>

<success_criteria>
- All tasks completed
- 8 modules defined with proper structure
- ModuleCard has satisfying hover interactions per CONTEXT.md
- Component is reusable and accepts Module type
</success_criteria>

<output>
After completion, create `.planning/phases/03-module-navigation/03-01-SUMMARY.md`
</output>
